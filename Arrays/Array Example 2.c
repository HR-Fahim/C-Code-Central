#include<stdio.h>
float Array(float A[],float B[],float , float );

void main()
{
    float p,q,X,Y;

    printf("Number of p:");
    (p);

    printf("Number of q:");
    q;


    printf("Identical",X);
    printf("Identical",Y);


}
float AArray(float A[],int size)
{

    int i,p;
    for(i=0;i<p;i++)
    {
    printf("Enter numbers:");
    scanf("%f",&A[i]);
    }
    return A[i];
}
float BArray(float B[],int size)
{
    int i,q;
    for(i=0;i<q;i++)
    {
    printf("Enter numbers:");
    scanf("%f",&B[i]);
    }
    return B[i];
}
float ABArray(float A[],float B[])
{
    int i,X,Y;
    (A[i],B[i]);
    if(A[i]==B[i])
        return X;
    else if (A[i]!=B[i])
        return Y;
}


